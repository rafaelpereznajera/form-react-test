import React, { Component } from 'react';
import FormInput from './FormInput';
class Form extends Component {
  render() {
    return (
      <form>
        Form
        <FormInput />
      </form>
    );
  }
}

export default Form;
